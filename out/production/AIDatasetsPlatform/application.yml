server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/adp?useSSL=false&serverTimezone=UTC
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      max-file-size: 1024MB
      max-request-size: 1024MB

file:
  upload-dir: F:\University\GraduationProject\AIDatasetsPlatform\src\main\resources\dataset\

logging:
  level:
    org.springframework.security: DEBUG
